//
// Created by longxing on 19-8-8.
//
#include <bits/stdc++.h>
using namespace std;
typedef  struct  people{
    int id;
    char sex;
    int fa;
    int ma;
}P;
int find(int id1, int id2){
    for (int i = 0; i < 5; ++i) {

    }

    return 1;
}
int main(){
    ios::sync_with_stdio(false);
    P p[10100];
    int n, m;
    cin >> n;
    for (int i = 0; i < n; ++i) {
         cin >> p[i].id >> p[i].sex >> p[i].fa >> p[i].ma;
    }

    return 0;
}
